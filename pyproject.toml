[project]
name = "llmstatistics"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.10.9",
    "numpy",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
