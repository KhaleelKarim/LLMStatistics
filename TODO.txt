PREVIOUS CODE
-Put together a report for threhsholding

UNDERSTANDING CODE

-Understanding the code further will require watching some videos on attention and transformers
-Look at 3Blue1Brown


MODIFICATIONS
-Feel free to speed up with numpy and other libraries
-Train the model several times and and save different versions (change the seed!)
-Try to do the following analyses with all different models, hope to see distributions are similar

TRAINING
-Train the model for much longer so that it sees more of the input data

OUTPUT
-Confirm what happens when temperature is approaching 0 or exactly 1

PROMPT
-Add the ability to prompt
-What happens when the prompt is too long or there is a BOS in middle? (Never seen during training?)

RECURSIVE BEHAVIOR
-What if you feed your outputted names into the model to produce more names?
-Do the dsitributions change? Do the names change?
-Does the model converge to something? Does it loop?

DISTRIBUTIONS:
-Compare input distribution, the distributions that the model understands (how it evolves over time), and the output distribution 

MARKOV CHAIN:
-Is there a state space we can use to model this as a markov chain?


PLANS:
-Input distribution has already been tracked, now track distributions as we train and after training
-Do this by taking a break every so often during training, generating 32,000 words, and computing distributions
-Once the model is fully trained do this as well 
-Use tmux or screen to keep this running, and also verify that GPU is speeding anything up at all